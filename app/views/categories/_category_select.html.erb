<%= select_tag(:category , options_for_select(all_categories, selected_value(@category)), :class => "select_style", :id=> "category_select", :prompt => "All Categories", 
  :data => {:remote => true, :url => home_path})%>

