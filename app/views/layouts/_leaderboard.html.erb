 <div id="leaderboard_back">

<div id="leaderboard">
 		
		<%= select_tag('category_id' , options_for_select(all_categories), :class => "select_style") %>
                <%= select_tag('leaderboard_id' , options_for_select(leaderboard_list(@category)), :class => "select_style") %>
         
		<div id="leaderboard_title"></div>
		
		<div id="backtotop"><a class="scroll" href="#header"></a></div>
		<div id="leader_all">
			
		
		<div class="clear"></div>
		</div>
       <div>
       <% @user_list.first(5).each_with_index do |user, i| %>
       <%= render :partial => "users/leader", :locals => {:user => user, :i => i}%>
       <% end %>
       </div>
</div>
<div id="footer">
<%=render :partial=>"layouts/footer"%>
</div>

</div>
