
<% if @videos.empty? %>

  
  <div id="empty_prompt"> 
         <% if @leaderboard != "All Leaderboards" && @leaderboard != nil %>
                <h1>This Leaderboard is empty = (</h1>
  		<h2><span><a onclick="window.open('/videos/new', 'newwindow', 'width=550, height=750, top=30, left=50'); return false;" href="#">Upload a video</a></span> to create one</h2> 



         <% else %>
  		<h1>No Leaderboards in this category.</h1>
  		<h2><span><a onclick="window.open('/videos/new', 'newwindow', 'width=550, height=750, top=30, left=50'); return false;" href="#">Upload a video</a></span> to create one</h2> 
         <% end %>
  </div>

<% else %> 

<% if @leaderboard != "All Leaderboards" && @leaderboard != nil %>
<div id="leaderboard_info">
<h3> <%= @leaderboard %> </h3>
<br />
<p> <%= @description %> <p> 
</div>
<% end %>

<% @videos.each_with_index do |video, i| %>

<div class="leader video">
			<h3><%= num_rank(i+1) %></h3>
			
			<div class="video_pic"> 
			<%= link_to(vid_bank_path(:video => video.id, :leaderboard => @leaderboard), :class => "leader_thumb_link", :remote => true) do %>   
     			<%= image_tag("http://img.youtube.com/vi/#{video.yt_video_id}/0.jpg", :height =>"100%", :width => "100%", :rel => "#{video.yt_video_id}") %>
     			<% end %>   
				
				
			</div>

		
			<div class="leader_info">
  		      <%= link_to(vid_swap_path(:video => video.id, :bank => "Top Videos"), 
               :method => :post, 
               "data_type" => :js, :remote=> true) do%>
			<h1><%= video.title %></h1>
                       <% end %>
			<h2>By: <%= link_to video.user.name, select_user_path(:user => video.user.id), :remote => true %></h2>
			<p>
			<%= video.description %>
			<br />
 		        Rep: <%= video.points %>
		
			</p>

			</div>
			<div class="leader_bottom"></div>
			<div class="leader_left"></div>
			<div class="leader_right"></div>

</div>
<% end %> <!-- end if/else -->

<%= paginate @videos, :params => {:controller => 'leaderboards', :action => 'video_board', :category => @category}  %>

<% end %>
