<div class="leader">
			<h3><%= num_rank(i+1) %></h3>
			
			<div class="leader_pic"> 
			<%= link_to("#", :class => "leader_thumb_link", :onclick => select_user(user), :remote => true) do %> 
     			<%= image_tag user.avatar.url(:large) %> 
     			<% end %>   
				
				
			</div>

		
			<div class="leader_info">
			<h1><%=user.name%></h1>
			<h2><%=user.major%></h2>	
			<p>
			<%=user.about%>	
			</p>
			</div>
			<div class="leader_divider"></div>
			<div class="leader_videos">
				<h2>Top Videos</h2>
				<h5>Title</h5>
				<h6>Pts</h6>
				<ol>
    <% user.videos.order("points DESC").each do |video| %>
    <%= link_to(vid_swap_path(:video => video.id), :method => :post, "data_type" => :js, :remote=> true, :onclick => "$('html,body').animate({scrollTop:$('#video_container h1').offset().top}, 500)") do%>
   <li><%= video.title %><span><%= video.points %>+</span></li><%end%>
   <% end %>
		
				</ol>
				<a class="leader_see_all" href="#">see all...</a>
   <%= link_to( "see all..." , vid_bank_path(:user_id => user.id), :class=>"leader_see_all", :method => :post, "data_type" => :js, :remote=> true, 
   :onclick => "$('html,body').animate({scrollTop:$('#video_container h1').offset().top}, 500);$('a.close').trigger('click');")%>
			</div>
			<div class="leader_bottom"></div>
			<div class="leader_left"></div>
			<div class="leader_right"></div>
		</div>
